<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smarthomebeta">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartHomeBeta">
        <activity
            android:name=".order"
            android:label="@string/title_activity_order"
            android:theme="@style/Theme.SmartHomeBeta.NoActionBar"></activity>
        <activity android:name=".newHouse" />
        <activity android:name=".newContact" />
        <activity android:name=".Contactmenu" />
        <activity android:name=".loginok" />
        <activity android:name=".auth" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET"/>

</manifest>